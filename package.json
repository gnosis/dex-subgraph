{
  "name": "dex-subgraph",
  "version": "0.1.0",
  "scripts": {
    "prepare:mainnet": "mustache config/mainnet.json subgraph.yaml.mustache > subgraph.yaml",
    "prepare:rinkeby": "mustache config/rinkeby.json subgraph.yaml.mustache > subgraph.yaml",
    "prepare:ganache": "mustache config/ganache.json subgraph.yaml.mustache > subgraph.yaml",
    "abi": "node src/scripts/fetchAbi",
    "addresses": "node src/scripts/printAddresses",
    "compile": "truffle compile",
    "codegen": "graph codegen",
    "build": "graph build",
    "create-ganache": "npm run prepare:ganache && graph create anxolin/dfusion --node http://127.0.0.1:8020",
    "create-rinkeby": "npm run prepare:rinkeby && graph create anxolin/dfusion_rinkeby --node https://api.thegraph.com/deploy/",
    "create-mainnet": "npm run prepare:mainnet && graph create anxolin/dfusion --node https://api.thegraph.com/deploy/",
    "deploy": "npm run prepare:ganache && graph deploy anxolin/dfusion --ipfs http://localhost:5001 --node http://127.0.0.1:8020",
    "deploy-rinkeby": "npm run prepare:rinkeby && graph deploy anxolin/dfusion_rinkeby --ipfs https://api.thegraph.com/ipfs/ --node https://api.thegraph.com/deploy/",
    "deploy-mainnet": "npm run prepare:mainnet && graph deploy anxolin/dfusion --ipfs https://api.thegraph.com/ipfs/ --node https://api.thegraph.com/deploy/",
    "lint": "eslint src",
    "lint:fix": "eslint --fix src"
  },
  "dependencies": {
    "@gnosis.pm/dex-contracts": "^0.1.3"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.16.0",
    "@graphprotocol/graph-ts": "^0.16.0",
    "babel-polyfill": "^6.26.0",
    "babel-register": "^6.26.0",
    "dotenv": "^8.2.0",
    "eslint": "^6.7.2",
    "eslint-config-prettier": "^6.7.0",
    "mustache": "^3.1.0",
    "truffle": "^5.1.3",
    "truffle-contract": "^4.0.31",
    "truffle-hdwallet-provider": "^1.0.4"
  }
}
