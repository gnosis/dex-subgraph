{
  "name": "dex-subgraph",
  "version": "0.2.0",
  "scripts": {
    "prepare:ganache": "mustache config/ganache.json subgraph.yaml.mustache > subgraph.yaml",
    "prepare:mainnet": "mustache config/mainnet.json subgraph.yaml.mustache > subgraph.yaml",
    "prepare:rinkeby": "mustache config/rinkeby.json subgraph.yaml.mustache > subgraph.yaml",
    "prepare:xdai": "mustache config/xdai.json subgraph.yaml.mustache > subgraph.yaml",
    "abi": "ts-node scripts/fetchAbi",
    "addresses": "ts-node scripts/printAddresses",
    "codegen": "graph codegen",
    "postinstall": "yarn prepare:ganache && yarn codegen",
    "build": "graph build",
    "create:ganache": "yarn prepare:ganache && graph create gnosis/protocol --node http://127.0.0.1:8020",
    "deploy:ganache": "yarn prepare:ganache && graph deploy gnosis/protocol --ipfs http://127.0.0.1:5001 --node http://127.0.0.1:8020",
    "deploy:rinkeby": "yarn prepare:rinkeby && graph deploy gnosis/protocol-rinkeby --ipfs https://api.thegraph.com/ipfs/ --node https://api.thegraph.com/deploy/",
    "deploy:staging": "yarn prepare:mainnet && graph deploy gnosis/protocol-staging --ipfs https://api.thegraph.com/ipfs/ --node https://api.thegraph.com/deploy/",
    "deploy:mainnet": "cli-confirm \"\n\nAre you sure the current version is deployed and tested in staging?\" && yarn prepare:mainnet && graph deploy gnosis/protocol --ipfs https://api.thegraph.com/ipfs/ --node https://api.thegraph.com/deploy/",
    "deploy:xdai": "yarn prepare:xdai && graph deploy gnosis/protocol-xdai --ipfs https://api.thegraph.com/ipfs/ --node https://api.thegraph.com/deploy/",
    "lint": "eslint --max-warnings 0 .",
    "lint:fix": "eslint --fix .",
    "ts:check": "tsc",
    "test": "mocha -r ts-node/register tests/**/*.test.ts",
    "test:e2e": "mocha -r ts-node/register e2e/tests/**/*.test.ts",
    "ci": "yarn lint && yarn build && yarn ts:check && yarn test"
  },
  "dependencies": {
    "@gnosis.pm/dex-contracts": "=0.4.3",
    "@openzeppelin/contracts": "=2.5.1"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "https://github.com/graphprotocol/graph-cli.git#5079eef",
    "@graphprotocol/graph-ts": "^0.18.1",
    "@types/chai": "^4.2.12",
    "@types/debug": "^4.1.5",
    "@types/mocha": "^8.0.3",
    "@types/node": "^12.12.59",
    "@types/node-fetch": "^2.5.7",
    "@typescript-eslint/eslint-plugin": "^4.1.1",
    "@typescript-eslint/parser": "^4.1.1",
    "chai": "^4.2.0",
    "cli-confirm": "^1.0.1",
    "debug": "^4.2.0",
    "eslint": "^7.9.0",
    "eslint-config-prettier": "^6.11.0",
    "eslint-plugin-prettier": "^3.1.4",
    "mocha": "^8.1.3",
    "mustache": "^4.0.1",
    "node-fetch": "^2.6.1",
    "prettier": "^2.1.1",
    "ts-node": "^9.0.0",
    "typescript": "^4.0.2",
    "wabt": "^1.0.19"
  }
}
